#include <iostream>
#include <vector>
#include <string>
#include <cassert>
#include "app.h"


int main() {
    assert(f({}) == 0);
    std::cout << "All test cases passed!" << std::endl;

    return 0;
}